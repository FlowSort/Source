import ExcelJS from "exceljs";import type { Prioridad } from "@flowsort/shared";
const colores:Record<Prioridad,string>={BAJA:"4C7A9E",PREDETERMINADA:"2DD4BF",ALTA:"F59E0B",SUPER_ALTA:"F43F5E"};
export async function crearExcel(registros:any[],usuarios:Map<string,string>){const wb=new ExcelJS.Workbook();const ws=wb.addWorksheet("Registros",{views:[{state:"frozen",ySplit:1}]});const dinamicos=new Map<string,string>();for(const r of registros)for(const c of r.plantilla.campos as any[])dinamicos.set(c.id,c.etiqueta);ws.columns=[{header:"Cliente",key:"cliente",width:28},{header:"Contacto",key:"contacto",width:24},{header:"Plantilla",key:"plantilla",width:22},{header:"Prioridad",key:"prioridad",width:18},{header:"Estado",key:"estado",width:18},{header:"Usuario",key:"usuario",width:26},{header:"Fecha de registro",key:"creado",width:22},{header:"Fecha de actualización",key:"actualizado",width:22},...[...dinamicos].map(([key,label])=>({header:label,key:`d_${key}`,width:24}))];for(const r of registros){const row:Record<string,unknown>={cliente:r.nombreCliente,contacto:r.contacto,plantilla:r.plantilla.nombre,prioridad:r.prioridad,estado:r.estado,usuario:usuarios.get(r.usuarioId)??r.usuarioId,creado:r.fechaRegistro,actualizado:r.fechaActualizacion};for(const [k,v] of Object.entries(r.especificaciones as object))row[`d_${k}`]=typeof v==="object"?JSON.stringify(v):v;const rr=ws.addRow(row);rr.getCell("prioridad").fill={type:"pattern",pattern:"solid",fgColor:{argb:colores[r.prioridad as Prioridad]}};}ws.getRow(1).eachCell(c=>{c.font={bold:true,color:{argb:"E6FFFA"}};c.fill={type:"pattern",pattern:"solid",fgColor:{argb:"10262A"}}});ws.autoFilter={from:"A1",to:ws.getRow(1).getCell(ws.columnCount).address};return Buffer.from(await wb.xlsx.writeBuffer())}
