import { Router } from "express";import { tenantDb } from "../services/tenant.js";import { ruta } from "../utils/async-route.js";
const router=Router();router.get("/",ruta(async(req,res)=>{const db=await tenantDb(req.auth!.empresaId);const soloDefault=req.query.soloDefault==="true";res.json(await db.plantilla.findMany({where:soloDefault?{esDefault:true}:undefined,orderBy:[{esDefault:"desc"},{nombre:"asc"}]}))}));export default router;
